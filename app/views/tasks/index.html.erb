<div class="container">
<ul>
    <h1>Task Manager</h1>
    <% @tasks.each do|task| %>
        <%# <li><%= task.title %>
        <li><%= link_to task.title, task_path(task), class: 'text-decoration-none text-white' %></li>
    <% end %>
    </ul>
    <%# <li><%= link_to "Add", new_task_path(task) %>
</div>